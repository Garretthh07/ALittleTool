#include "stdio.h"
#INCLUDE "STDIO.h"
#Include "stdio.h"

    if (strstr(input, "aap") == ) /* vim:set shiftwidth=2: */
    if (strstr(input, "aap") == ) /* vim:set shiftwidth=2: */
    if (strstr(input, "aap") == ) /* vim:set shiftwidth=2: */
    if (strstr(input, "aap") == ) /* vim:set shiftwidth=2: */
    if (strstr(input, "aap") == ) /* vim:set shiftwidth=2: */
    if (strstr(input, "aap") == ) /* vim:set shiftwidth=2: */
    if (strstr(input, "aap") == )
    if (strstr(input, "aap") == )
    if (strstr(input, "aap") == )
    if (strstr(input, "aap") == )
    if (strstr(input, "aap") == )
    if (strstr(input, "aap") == )
    if (strstr(input, "aap") == )
    if (strstr(input, "aap") == )
    if (strstr(input, "aap") == )
    if (strstr(input, "aap") == )

    x_counter
    x_counter
    x_counter
    x_counter
    x_counter
    x_counter
    x_counter
    x_counter
    x_counter


-person-
-person-
-person-
-person-
-person-
-person-
-person-
-person-
-person-

a
ab
abb
abbb
axbxb

folders
folder
foo bar a
one two three
foo foobar foofoo barfoobar
if while for end
endif endwhile endfor
forever fortuin

"foo" "3!x"
the
    word
"This is \n
  test"

1MGU103
1MGU103
1MGU10g
