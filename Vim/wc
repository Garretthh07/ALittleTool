2015年 2月 8日 星期日 16时16分58秒 UTC
line 1
2015年 2月 9日 星期一 00时14分34秒 CST
.DS_Store
.git
A_Byte_of_Python
a.py
collection
output.txt
tempo
line 4
line 5
line 5
line 6
line 7


# Filename: if.py
#!/usr/bin/python
number = 23 guess = int(raw_input('enter an integer : '))
if guess == number: print 'Congratulation, you guessed it.' # New block
starts here print "(but you do not win any prizes!" # New block ends
here elif guess < number: print 'No, it is a  little higher than that'
#Another block #you can do whatever you want in a block ...  else: print
'No, it is a little lower than that' #you must have guess > number to
reach here print 'Done'
# This last statement is always executed, after the if statement is
# executed

//Chapter abcdefghijklmnopqrstuvwxyz zyxwvutsrqponmlkjihgfedcba
//1234567890-_=+p{[}]p[]nm,<.>//?AI\|` `~.......  #include "stdio.h"
ds//#include "fcntl.h" #!/usr/bin/python
# Filename: if.py

number = 23 guess = int(raw_input('Enter an integer : '))

if guess == number: print 'Congratulation, you guessed it.' # New block
starts here print "(but you do not win any prizes!" # New block ends
here elif guess < number: print 'No, it is a  little higher than that'
#Another block #you can do whatever you want in a block ...  else: print
'No, it is a little lower than that' #you must have guess > number to
reach here print 'Done'
# This last statement is always executed, after the if statement is
# executed

//#include "unistd.h" #include "stdlib.h"
//
foobar
//
//Teacher weishun criticized Professor Johnson today.  Chapter
//
string//barfoo
//barfoo
//
//barm     xEW TEXt.//barm     xEW TEXt.//         xEW TEXt.//bar#
//xEW TEXt.//barf     xEW TEXt.//         xEW TEXt.//barf     xEW
//TEXt.//barf     xEW TEXt.
#!/usr/bin/python
# Filename: if.py

number = 23 guess = int(raw_input('Enter an integer : '))

if guess == number: print 'Congratulation, you guessed it.' # New block
starts here print "(but you do not win any prizes!" # New block ends
here elif guess < number: print 'No, it is a  little higher than that'
#Another block #you can do whatever you want in a block ...  else: print
'No, it is a little lower than that' #you must have guess > number to
reach here print 'Done'
# This last statement is always executed, after the if statement is
# executed

